/// An enum of Long-Acting Reversible Contraceptives (LARCs) types.
/// Currently only injection is supported, but more may be added in the future.
enum LarcTypes {
  iud,
  implant,
  injection,
  ring,
  patch,
}